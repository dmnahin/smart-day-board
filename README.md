# Smart Day Board

Smart Day Board is a beginner-friendly web project for GitHub. It combines a mini calendar, a quick calculator, a habit tracker, and a daily note.

## Files

- `index.html` contains the page structure.
- `style.css` contains the design.
- `script.js` contains the app logic.

## How to Open

Open `index.html` in a browser.

## How to Put on GitHub

1. Go to https://github.com and sign in.
2. Click the `+` button in the top-right corner.
3. Click `New repository`.
4. Repository name: `smart-day-board`.
5. Choose `Public`.
6. Tick `Add a README file` if GitHub shows that option.
7. Click `Create repository`.
8. Click `Add file`, then `Upload files`.
9. Upload these project files: `index.html`, `style.css`, `script.js`, and `README.md`.
10. Click `Commit changes`.

## Show It as a Website

1. In your repository, click `Settings`.
2. Click `Pages` in the left menu.
3. Under `Build and deployment`, choose `Deploy from a branch`.
4. Select branch `main` and folder `/root`.
5. Click `Save`.
6. Wait one or two minutes. GitHub will show your website link.

## Ideas to Improve

- Add dark mode.
- Save habit checkboxes in the browser.
- Add custom events to the calendar.
- Add more calculator options.
